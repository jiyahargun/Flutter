import 'package:flutter/material.dart';

class CustomColors {
  const CustomColors._();

  // Main brown (AppBar, cards)
  static const Color primaryColor = Color(0xFF7A5646);

  // Thoda light brown (cards / highlights)
  static const Color primaryLightColor = Color(0xFF9C6B55);

  // Background light grey
  static const Color greyColor = Color(0xFFF2F2F2);

  static const Color whiteColor = Colors.white;
  static const Color blackColor = Colors.black87;

  // Danger / delete
  static const Color dangerColor = Color(0xFFE53935);
}
